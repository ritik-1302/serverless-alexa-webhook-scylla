plugins:
 - serverless-offline

service: serverless-alexa-webhook-scylla
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-south-1
  memorySize: 128

functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: post
